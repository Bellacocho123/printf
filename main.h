#ifndef MAIN_H
#define MAIN_H

/* Importing standard C lib */

#include <unistd.h>
#include <stdarg.h>

/* function prototypes */
int _printf(const char *format, ...);
int _puts(char *string);
int _putchar(char z);
int _my_puts(char *string);

#endif /* MAIN */
